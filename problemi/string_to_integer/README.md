# String To Integer
Write a program that receives a sequence of space-separated values from the user as input and saves them, as strings, in a vector V.
Then, attempt to convert the elements of V into integers.
The program must convert 'valid' numbers into integers and remove any strings from V that cannot be converted and then print V.